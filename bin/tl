#!/usr/bin/env ruby

require "benchmark"
require_relative "../lib/task-list"

time = Benchmark.realtime do
  TaskList::Parser.new(*ARGV)
end

puts "\n" + ("-" * 30) + "\nTime elapsed: #{time * 1000} milliseconds"
